{
  "name": "chaindb",
  "version": "0.2.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "yarn workspaces foreach -tvp run build",
    "watch": "yarn workspaces foreach -p run watch",
    "start": "yarn workspace @chaindb/main start"
  },
  "devDependencies": {
    "@types/node": "^20.3.0",
    "typescript": "^5.1.3"
  }
}
